# Claude Code Guidelines

## Git

- Never use `git add`, `git commit`, or `git push`.
- Never stage, commit, or push changes under any circumstances.
- Always stop after making file changes and tell the user exactly what files were changed so they can handle git themselves in GitHub Desktop.
